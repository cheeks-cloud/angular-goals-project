import { DateCouuntPipe } from './date-couunt.pipe';

describe('DateCouuntPipe', () => {
  it('create an instance', () => {
    const pipe = new DateCouuntPipe();
    expect(pipe).toBeTruthy();
  });
});
